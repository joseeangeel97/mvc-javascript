#Un pequeño programa que añade en la pantalla elementos que se guardan en un array a traves de un formulario, y muestra un contador de los elementos en pantalla   7
#Posee un array con informacion oculta que aparece si se acierta el codigo el cual es, se usa expresiones regulares para mostrar la informacion por pantalla


#A little program, which show in the broswer a few dynamics elements, which are saved in an array, 
#also there are hide information that you can guess and show in the window, use regular expressions to introduce the data
